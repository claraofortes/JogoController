import React from "react"
import { Outlet } from "react-router-dom"

export const PlatformEditor: React.FC = () => {
    return (
        <div>
            <p>Platform Editor</p>
        </div>
    );
}